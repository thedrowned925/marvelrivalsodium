name: Sync live tracker data (manual compatibility)

# The v2 workflow is the only scheduler and writer. Keep old manual links usable.
on:
  workflow_dispatch:
permissions:
  contents: write
jobs:
  sync:
    uses: ./.github/workflows/sync-data-v2.yml
